# Puppet environment configuration file

# This file configures the Puppet environment settings, including module paths and environment-specific configurations.

modulepath = modules:site:vendor
environment = production
manifest = manifests/init.pp
```